import { connect } from "react-redux";
import Traveller from '../../views/Traveller/Traveller';

const mapDispatchToProps = dispatch => ({});
const mapStateToProps = state => ({});

export default connect(mapStateToProps, mapDispatchToProps)(Traveller);